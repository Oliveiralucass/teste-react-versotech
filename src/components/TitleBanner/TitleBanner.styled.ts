import styled from 'styled-components'

export const TitleBannerStyled = styled.section`
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    height: 60px;
    width: 100%;
    border-bottom: 1px solid var(--color-primary-light);
    color: var(--color-primary-light);
`